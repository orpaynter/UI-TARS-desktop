# Security cleanup

This file was removed from its prior secret-bearing form and replaced with a neutral placeholder.

Do not commit real credentials to source control.

Use a secret manager or environment variables injected at runtime instead.

Examples:
- .env.local (gitignored)
- OS keychain / secret manager
- CI/CD encrypted secrets
