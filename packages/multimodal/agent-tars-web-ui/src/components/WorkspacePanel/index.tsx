import React from 'react';
import { WorkspacePanelContainer } from './WorkspacePanelContainer';

// Re-export the main component for backward compatibility
export const WorkspacePanel = WorkspacePanelContainer;
